FROM ubuntu:22.04

RUN apt-get update && \
  apt-get install -y gpg wget && \
  rm -rf /var/lib/apt/lists/*

RUN apt-get update && \
  apt-get install -y \
    distcc \
    gcc && \
  rm -rf /var/lib/apt/lists/*